<!doctype html>
<html>
<head>
    <title>{{ _title }} - Verbannte Bücher -  Berlin.de</title>
    <meta charset="UTF-8" />    
    {% set locale = tm.getCurrentLocale() %}
    {% set dir = locale.getCharacterOrientation() == 'right-to-left' ? 'rtl' : 'ltr' %}
    {% set is_error_page = TRUE == rq.getAttribute('is_error_page') %}
    <base href="{{ ro.getBaseHref() }}" id="base_href"/>
    {% include ac('include.base_path') ~ ac('include.imperia_head') %}
    <!-- %%STYLESHEETS%% -->

   
   </head>
   <body data-spy="scroll" data-target=".famport.content-navi-left" data-offset="100" class="{{_body_class}}">
        <!-- Include for portal header -->
        {% include ac('include.base_path', '') ~ ac('include.imperia_top') %}
            <!-- *********************************************************
                    MAIN CONTENT AREA (WHITE BG)
            ********************************************************** -->
                {{ slots.header|raw }}
                <div class="row">
                    <div class="span2 column-left">
                        {% include "navigation.twig" %}
                    </div><!--
                    --><div class="span7 column-content">
                        {{ inner|raw }}
                    </div><!--
                    --><div class="span3 column-right html5-aside" role="complementary">
                        <div class="html5-section content-marginal">
                            <div class="html5-section body">
                                <div class="html5-section block boxmerge">
                                    <div class="html5-header header">
                                        <h3 class="title">Liste in Datenform</h3>
                                    </div><!-- /html5-header -->
                                    <div class="html5-section body">
                                        <p>
                                            Die Liste der verbotenen Bücher erhalten Sie in Datenform im
                                            <a href="http://daten.berlin.de/datensaetze/liste-der-verbannten-bücher">Datenportal des Landes Berlin</a>
                                        </p>
                                        <p>
                                            <a href="http://daten.berlin.de/datensaetze/liste-der-verbannten-bücher">
                                                <img src="{{ro.getBaseHref()}}images/opendatalogo_small.png" />
                                            </a>
                                        </p>
                                    </div><!-- /html5-section -->
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                {{ slots.footer|raw }}
        <!-- Include for portal footer -->
        {% include ac('include.base_path') ~ ac('include.imperia_foot') %}
        <!-- %%JAVASCRIPTS%% -->
        <script src="http://service.berlin.de/feedback/js.php"></script>
    </body>
</html>
