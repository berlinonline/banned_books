<h1>Bücher A-Z</h1>
<div>
    <ul class="azlist-pager">
    {% for letter, link in letter_links %}

        <li class="{% if not link %}inactive{% endif %}">
            <a {% if link %}href="{{link}}"{% endif %}>{{letter}}</a>
        </li>
    {% endfor %}
    </ul>
</div>

<div class="azlist">
    <h2 class="azlist-letter" id="id_A">{{letter}}</h2>
    <ul class="list">
    {% for book in books %}
        <li class="{{loop.index0}}">
            <a href="{{book.url}}">
                {{book.title}}
            </a>
        </li>
    {% endfor %}
    </ul>
</div>
